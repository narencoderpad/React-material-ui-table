{
  "name": "react-select",
  "description": "Unsure why I can only select one object, and mouse-over doesn't highlight the item any longer.",
  "version": "0.0.2",
  "dependencies": {
    "@material-ui/core": "^4.9.2",
    "@material-ui/icons": "3.0.1",
    "react": "^16.7.0",
    "react-dom": "^16.8.4"
  },
  "devDependencies": {
    "react-scripts": "1.0.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
